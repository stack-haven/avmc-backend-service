# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ChatService API
    description: 对话管理服务
    version: 0.0.1
paths:
    /ai/v1/ai/chats:
        get:
            tags:
                - ChatService
                - 对话管理服务
            summary: 获取对话列表分页
            description: 获取对话列表分页
            operationId: ChatService_ListChats
            parameters:
                - name: pageSize
                  in: query
                  description: 每页的行数
                  schema:
                    type: integer
                    format: int32
                - name: pageToken
                  in: query
                  description: 当前页码
                  schema:
                    type: string
                - name: nopaging
                  in: query
                  description: 是否不分页
                  schema:
                    type: boolean
                - name: status
                  in: query
                  schema:
                    enum:
                        - STATUS_UNSPECIFIED
                        - STATUS_ENABLED
                        - STATUS_DISABLED
                    type: string
                    format: enum
                - name: filter
                  in: query
                  description: |-
                    Optional. The standard list filter.
                     Supported fields:
                        * `name` (i.e. `name="John Doe"`)
                        * `email` (i.e. `email="admin@go-kratos.dev"`)
                        * `phone` (i.e. `phone="+1234567890"`)
                        * `create_time` range (i.e. `timestamp>="2025-01-31T11:30:00-04:00"` where
                        the timestamp is in RFC 3339 format)

                     More detail in [AIP-160](https://google.aip.dev/160).
                  schema:
                    type: string
                - name: orderBy
                  in: query
                  description: |-
                    Optional. A comma-separated list of fields to order by, sorted in ascending
                     order. Use "desc" after a field name for descending. Supported fields:
                     - `create_time`

                     Example: `create_time desc`.
                  schema:
                    type: string
                - name: skip
                  in: query
                  description: |-
                    * A request with no page token and a skip value of 30 returns a single
                       page of results starting with the 31st result.
                     * A request with a page token corresponding to the 51st result (because the
                       first 50 results were returned on the first page) and a skip value of 30
                       returns a single page of results starting with the 81st result.
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListChatsResponse'
            security:
                - BearerAuth: []
        post:
            tags:
                - ChatService
                - 对话管理服务
            summary: 创建对话
            description: 创建对话
            operationId: ChatService_CreateChat
            parameters:
                - name: operatorId
                  in: query
                  schema:
                    type: integer
                    format: uint32
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Chat'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateChatResponse'
            security:
                - BearerAuth: []
    /ai/v1/ai/chats/simple:
        get:
            tags:
                - ChatService
                - 对话管理服务
            summary: 获取对话简单列表
            description: 获取对话简单列表
            operationId: ChatService_ListChatsSimple
            parameters:
                - name: pageSize
                  in: query
                  description: 每页的行数
                  schema:
                    type: integer
                    format: int32
                - name: pageToken
                  in: query
                  description: 当前页码
                  schema:
                    type: string
                - name: nopaging
                  in: query
                  description: 是否不分页
                  schema:
                    type: boolean
                - name: status
                  in: query
                  schema:
                    enum:
                        - STATUS_UNSPECIFIED
                        - STATUS_ENABLED
                        - STATUS_DISABLED
                    type: string
                    format: enum
                - name: filter
                  in: query
                  description: |-
                    Optional. The standard list filter.
                     Supported fields:
                        * `name` (i.e. `name="John Doe"`)
                        * `email` (i.e. `email="admin@go-kratos.dev"`)
                        * `phone` (i.e. `phone="+1234567890"`)
                        * `create_time` range (i.e. `timestamp>="2025-01-31T11:30:00-04:00"` where
                        the timestamp is in RFC 3339 format)

                     More detail in [AIP-160](https://google.aip.dev/160).
                  schema:
                    type: string
                - name: orderBy
                  in: query
                  description: |-
                    Optional. A comma-separated list of fields to order by, sorted in ascending
                     order. Use "desc" after a field name for descending. Supported fields:
                     - `create_time`

                     Example: `create_time desc`.
                  schema:
                    type: string
                - name: skip
                  in: query
                  description: |-
                    * A request with no page token and a skip value of 30 returns a single
                       page of results starting with the 31st result.
                     * A request with a page token corresponding to the 51st result (because the
                       first 50 results were returned on the first page) and a skip value of 30
                       returns a single page of results starting with the 81st result.
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListChatsResponse'
            security:
                - BearerAuth: []
    /ai/v1/ai/chats/status-update/{id}:
        put:
            tags:
                - ChatService
                - 对话管理服务
            summary: 更新对话状态
            description: 更新对话状态
            operationId: ChatService_UpdateChatByStatus
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: uint32
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateChatByStatusRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateChatByStatusResponse'
            security:
                - BearerAuth: []
    /ai/v1/ai/chats/{id}:
        get:
            tags:
                - ChatService
                - 对话管理服务
            summary: 获取对话数据
            description: 获取对话数据
            operationId: ChatService_GetChat
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: uint32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Chat'
            security:
                - BearerAuth: []
        put:
            tags:
                - ChatService
                - 对话管理服务
            summary: 更新对话
            description: 更新对话
            operationId: ChatService_UpdateChat
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: uint32
                - name: operatorId
                  in: query
                  schema:
                    type: integer
                    format: uint32
                - name: updateMask
                  in: query
                  description: Required. Mask of fields to update.
                  schema:
                    type: string
                    format: field-mask
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Chat'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateChatResponse'
            security:
                - BearerAuth: []
        delete:
            tags:
                - ChatService
                - 对话管理服务
            summary: 删除对话
            description: 删除对话
            operationId: ChatService_DeleteChat
            parameters:
                - name: id
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: uint32
                - name: operatorId
                  in: query
                  schema:
                    type: integer
                    format: uint32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteChatResponse'
            security:
                - BearerAuth: []
components:
    schemas:
        Chat:
            type: object
            properties:
                id:
                    type: integer
                    description: 对话ID
                    format: uint32
                name:
                    type: string
                    description: 对话名
                status:
                    enum:
                        - STATUS_UNSPECIFIED
                        - STATUS_ENABLED
                        - STATUS_DISABLED
                    type: string
                    description: 状态 0 未知 1 启用 2 禁用
                    format: enum
                createdAt:
                    type: string
                    description: 创建时间
                updatedAt:
                    type: string
                    description: 更新时间
                description:
                    type: string
                    description: 个人说明
        CreateChatResponse:
            type: object
            properties: {}
        DeleteChatResponse:
            type: object
            properties: {}
        ListChatsResponse:
            type: object
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/Chat'
                total:
                    type: integer
                    format: int32
                nextPageToken:
                    type: string
        UpdateChatByStatusRequest:
            type: object
            properties:
                id:
                    type: integer
                    description: 对话ID
                    format: uint32
                status:
                    enum:
                        - STATUS_UNSPECIFIED
                        - STATUS_ENABLED
                        - STATUS_DISABLED
                    type: string
                    description: 状态 0 未知 1 启用 2 禁用
                    format: enum
                operatorId:
                    type: integer
                    description: 操作人ID
                    format: uint32
            description: 更新对话状态 - 请求
        UpdateChatByStatusResponse:
            type: object
            properties: {}
            description: 更新对话状态 - 回应
        UpdateChatResponse:
            type: object
            properties: {}
tags:
    - name: ChatService
